
@{
    ViewData["Title"] = "PrintCountryListUsingViewData";
}

<h2>Country List Using ViewData ===> ViewData["Countries"]</h2>

<ul>
    @foreach (string country in (List<string>)ViewData["Countries"])
    {
        <li>@country</li>
    }
</ul>

